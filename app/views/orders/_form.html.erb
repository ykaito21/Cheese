<%= simple_form_for [@dish, @order] do |f| %>
  <%= f.input :quantity, selected: f.object.quantity || '1',  collection: 1..20 %>
  <%= f.input :date, id: "order_date" %>
  <%= f.button :submit, "Order", class: "btn btn-warning btn-block"%>
<% end %>
